# Secrets for AcademicOS. NEVER commit this file.
# Loaded by academicos.config at startup; real environment variables win.

# Composio (email delivery through the school's connected Gmail/Outlook).
# NOTE: the key below was rejected with 401 "Invalid API key" — it looks
# truncated (23 chars). Replace with the full key from app.composio.dev.
COMPOSIO_API_KEY=ck_i1OxJQxa3cWW0d79zoeD

# SMTP fallback — works without any third party.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
